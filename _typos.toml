[files]
extend-exclude = [
    "Cargo.lock",
    "target/",
]

# Pinned hash anchor from the tests in src/lib.rs. typos tokenizes hex on
# digit boundaries and would otherwise flag the `ba` substring as a
# misspelling of `by` / `be`.
[default.extend-identifiers]
e25ba02c5e53651f = "e25ba02c5e53651f"
